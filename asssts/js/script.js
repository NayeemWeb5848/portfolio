/*============================================= Auto Type Start ========================================= */
var typed = new Typed(".typing",{
     strings:["Web Desinger","Web Developer", "Video Editor","Contant Creator"],
     typeSpeed:150,
     BackSpeed:60,
     loop:true
})

$(function(){
     'use strict';
     $('.venobox').venobox()
});
